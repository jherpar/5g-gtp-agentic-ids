# Pip-installable equivalent of pyproject.toml/poetry.lock, for readers who
# don't use Poetry. Exact versions pinned from poetry.lock (the versions
# this project's reported results were actually produced with) --
# `poetry install` (see ../README.md) remains the primary, authoritative
# setup path; this file is a convenience alternative, not a replacement.
#
# Usage:
#   python3.11 -m venv .venv && source .venv/bin/activate   # or .venv\Scripts\activate on Windows
#   pip install -r reproducibility/requirements.txt

# --- runtime dependencies ---
scapy==2.7.0
pydantic==2.13.4
pydantic-settings==2.14.2
pandas==2.3.3
pyarrow==18.1.0
numpy==1.26.4
scikit-learn==1.9.0
xgboost==2.1.4
plotly==6.9.0
pyyaml==6.0.3
requests==2.34.2
typer==0.27.0
tqdm==4.70.0
psutil==6.1.1
kaleido==1.3.0

# --- optional: PyShark parser backend (requires tshark/Wireshark on PATH;
#     the primary Scapy backend has no such dependency, this is optional) ---
# pyshark>=0.6,<0.7

# --- dev / test tooling ---
pytest==8.4.2
pytest-cov==6.3.0
ruff==0.8.6
black==24.10.0
mypy==1.20.2
ipykernel==6.31.0
types-pyyaml==6.0.12.20260724
